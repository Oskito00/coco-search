1. Decide how I am going to train model data. I believe it should be interaction based, did that account interact/buy that item? dismissed. The item relevance seems like an extra click to get feedback. The goal is to train an ML model to determine would the person want to be notified of this item based on the features we have access to
2. Investigating and cleaning up recent_scrape and full_scrape, are they both needed. The goal is to notify users of new items, hence why I implemented the recent scrape(ordered by date uploaded) but other things like price drops. Are useful to notify, This is why the full scrape was built. How can we achieve this plan in the cleanest most modularised way possible? The system overview says these funcitons are byte identical, this needs to be checked though, I thought one called without the page limit
3. Item processes seems like a clean workflow, is the code clean and modularised. Does the API payload response need to be re-examined. Item processing should not own as much as it currently does unless its an orchestrator. There should be things like search response parser, Client filter (optional if the user has put required words and excluded words in the search, price bounds? I thought these were handled in the query but I might be wrong. Notifiactions should be in its own class potentially with many subclasses each handling one thing. First search run should not notify most, but perhaps we should expose new endpoints to get recent items for search in the API.
4. Notifiactions and Account connection should be handled cleaner than it is now, there should be a dedicated Telegram class which handles telegram connection, maybe even with sub classes. Eventually I want to add more methods of notifications, like google chat, whatsapp. So the architecture should be made for this. One parent class like NotificationMethods, MessagingConnectors, or whatever you see fit.
5. Relevance layer should be completely isolated in its own class and subclasses. Maybe we call that layer relevance, as it is, that will hold the model, how to interact with the user feedback database. 
